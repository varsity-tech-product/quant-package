pandas>=2.0
numpy>=1.24
# 实盘下单 + universe 抓取
aiohttp>=3.9
apscheduler>=3.10
python-dotenv>=1.0
# 风格归因（quantkit.style_attribution / quantkit.attribution）画图
matplotlib>=3.7
# 回测客户端只用标准库 urllib，无需额外依赖
# 数据服务：仅依赖系统已安装 grpcurl（参考客户端 + proto 已内置于 quantkit/data/_gateway）
