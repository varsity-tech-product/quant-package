# 复制为 .env 并填写。.env 已在 .gitignore 中，不会被提交。

# ── 币安 Futures 下单 ──────────────────────────────────────────────
BINANCE_API_KEY=your_key_here
BINANCE_API_SECRET=your_secret_here
# 强烈建议先 testnet 验证（true），确认无误再切 false 上主网
BINANCE_TESTNET=true

# ── 数据服务 exchange-gateway ─────────────────────────────────────
# 数据服务 gRPC 地址（prod 默认）；dev 走 SSH tunnel 后用 127.0.0.1:8877
GATEWAY_TARGET=13.231.65.185:8777
# exchange-gateway 仓库根目录（含 proto，供 grpcurl 用）
EXCHANGE_GATEWAY_DIR=/home/ec2-user/exchange-gateway
# 每次拉多少根 1d bars
KLINE_LOOKBACK=1000

# ── universe = CMC Top N（universe.mode=cmc_top_n 时需要）──────────
CMC_API_KEY=
CMC_TOP_N=10

# ── 风控（可选，留空用默认）───────────────────────────────────────
LEVERAGE=1
MAX_SINGLE_WEIGHT=0.40
MIN_ORDER_NOTIONAL=20.0
MAX_DRAWDOWN_HALT=-0.20
LIMIT_ORDER_TIMEOUT=60

# ── 日度调仓时间（UTC）────────────────────────────────────────────
REBALANCE_HOUR_UTC=0
REBALANCE_MINUTE_UTC=5
